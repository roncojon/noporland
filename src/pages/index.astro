---
import Layout from '../layouts/Layout.astro';
import Card from '../components/Card.astro';
import type { VideoAndThumbnailUrlType } from '../env';
import SearchWithQueryProvider from '../components/Search/SearchWithQueryProvider';
import SiteIcon from '../components/SiteIcon.astro';

// const itemsByPage = 3
// const videosAndThumbnailUrlsProcesed: VideoAndThumbnailUrlType[] = await fetch(`https://0wy4drz3l6.execute-api.us-west-1.amazonaws.com/prod/%7Bvideos%7D?elementsPerPage=${itemsByPage}`)
//     .then(res => res.json())
//     .then(data => data.videoFiles?.map(item => ({
//         ...item,
//         videoKey: item?.videoKey?.slice(11)
//     })))
//     .catch(err => console.error(err)) ?? [];


// const availableTags:string[] = videosAndThumbnailUrlsProcesed.flatMap(e=>e.tags);

/* ["big ass", "maid", "shorts", "big ol tag to test", "another long tag gaaaaa"]; */ 

---

<Layout title="Welcome to NoporLand.">
	<main>
		<SiteIcon />
		<h1>Welcome to <span class="text-gradient">NoporLand</span></h1>
		<p class="instructions">
			To get started, open the directory <code>src/pages</code> in your project.<br />
			<strong>Code Challenge:</strong> Tweak the "Welcome to Astro" message above.
		</p>
		<SearchWithQueryProvider client:load/> <!-- tags={availableTags} -->
		<!-- <div id="link-card-grid-container" class=" relative">
			<div id="link-card-grid-container-background"></div>
		<ul role="list" class="link-card-grid" id="video-list">
			{videosAndThumbnailUrlsProcesed.map(({ videoKey, videoUrl, thumbnailUrl, previewUrl }) => (
				<Card
					href={videoUrl}
					title={videoKey}
					body="Watch the video"
					thumbnailUrl={thumbnailUrl}
					previewUrl={previewUrl}
				/>
			))}
		</ul>
	</div> -->

	 <!-- Placeholder for Load More button -->
	 <!-- <div id="pagination-controls">
		<button id="load-more-btn" class="load-more-btn" disabled>Loading...</button>
	  </div> -->
	</main>
</Layout>

<!-- Add client-side script to handle pagination -->
<!-- <script src="../scripts/pagination.js" ></script> -->

<style>
	main {
		margin: auto;
		padding: 1rem;
		/* width: 800px; */
		max-width: calc(100% - 60px);
		color: white;
		font-size: 20px;
		line-height: 1.6;
	}
	.astro-a {
		position: absolute;
		top: -4px;
		left: 160px;
		transform: translatex(-50%);
		width: 220px;
		height: auto;
		z-index: -1;
		filter: brightness(50%) blur(4px);
		opacity: 0.6;
	}
	h1 {
  font-size: 3rem;
  font-weight: 700;
  line-height: 1;
  text-align: center;
  margin-bottom: 0.5em;
  text-shadow: 2px 2px 14px #25082d;

}

.text-gradient {
  background-image: linear-gradient(
    10deg,
    rgba(224, 204, 250, 0.8), /* Lighter version of accent-light */
    rgba(96, 38, 174, 0.8) 30%, /* Lighter version of accent */
    rgba(224, 204, 250, 0.4) 90% /* Much lighter at the end */
  );
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-size: 800%;
  background-position: 0%;
}

	.instructions {
		margin-bottom: 2rem;
		border: 1px solid rgba(var(--accent-light), 25%);
		background: linear-gradient(rgba(var(--accent-dark), 66%), rgba(var(--accent-dark), 33%));
		padding: 1.5rem;
		border-radius: 8px;
	}
	.instructions code {
		font-size: 0.8em;
		font-weight: bold;
		background: rgba(var(--accent-light), 12%);
		color: rgb(var(--accent-light));
		border-radius: 4px;
		padding: 0.3em 0.4em;
	}
	.instructions strong {
		color: rgb(var(--accent-light));
	}
	.link-card-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(250px, 0.5fr));
		
		gap: 0.5rem;
		padding: 0;
	}

	@media (max-width: 768px) {
		main {
			max-width: none;
		}
       .link-card-grid {
           grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
       }
   }   

   .load-more-btn {
    display: block;
    margin: 2rem auto;
    padding: 0.75rem 1.5rem;
    background-color: #9632a8;
    color: white;
    border: none;
    border-radius: 5px;
    cursor: pointer;
  }

  .load-more-btn:hover {
    background-color: #b340ca;
  }

  .no-more-videos {
    text-align: center;
    margin: 2rem 0;
  }

</style>
